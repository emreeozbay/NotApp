@model NotApp.Models.NoteItem
@{
    ViewData["Title"] = "Not Yükle";
}

<h1 class="h3 mb-3">Not Yükle</h1>

<div class="card card-glass p-4">
  <form asp-action="Create" method="post" enctype="multipart/form-data">
    @Html.AntiForgeryToken()

    <div class="row g-3">

      <div class="col-12">
        <label class="form-label">Başlık</label>
        <input asp-for="Title" class="form-control" required />
        <span class="text-danger" asp-validation-for="Title"></span>
      </div>

      <div class="col-12">
        <label class="form-label">Açıklama</label>
        <textarea asp-for="Description" class="form-control" rows="3"></textarea>
      </div>

      <div class="col-12">
        <label class="form-label">Dosya</label>
        <input type="file" name="file" class="form-control" />
      </div>

      <div class="col-md-6">
        <label class="form-label">Fakülte</label>
        <select class="form-select" asp-for="Faculty" data-faculty>
          <option value="">Fakülte Seçiniz</option>
          <!-- JS dolduracak -->
        </select>
      </div>

      <div class="col-md-6">
        <label class="form-label">Bölüm</label>
        <select class="form-select" asp-for="Department" data-department disabled>
          <option value="">Önce Fakülte Seçiniz</option>
        </select>
      </div>

      <div class="col-md-4">
        <label class="form-label">Sınıf</label>
        <select asp-for="ClassLevel" class="form-select">
          <option value="">Sınıf Seç</option>
          <option>1. Sınıf</option><option>2. Sınıf</option>
          <option>3. Sınıf</option><option>4. Sınıf</option>
        </select>
      </div>

      <div class="col-md-4">
        <label class="form-label">Ders Kodu</label>
        <input asp-for="CourseCode" class="form-control" />
      </div>

      <div class="col-md-4">
        <label class="form-label">Ders Adı</label>
        <input asp-for="CourseName" class="form-control" placeholder="Örnek: Veri Yapıları" />
      </div>

      <div class="col-12 d-flex justify-content-end">
        <a asp-action="Index" class="btn btn-outline-light me-2">Vazgeç</a>
        <button class="btn btn-danger">Yükle</button>
      </div>
    </div>
  </form>
</div>

@section Scripts{
  <partial name="_ValidationScriptsPartial" />
}
